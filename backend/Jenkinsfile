pipeline {
    agent any

    stages {
        stage('Build') {
            agent {
                dockerfile {
                    filename 'Dockerfile'
                    dir 'backend'
                }
            }

            steps {
                sh '''
                    node --version
                    ls -la
                    docker images
                '''
            }
        }
    }
}