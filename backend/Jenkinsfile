pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                sh '''
                    docker --version
                    docker build -t nestjs-app ./backend
                    docker images
                    docker run --rm -p 3000:3000 nestjs-app
                '''
            }
        }
    }
}