pipeline {
    agent any

    stages {
        stage('Build') {
            agent {
                dockerfile {
                    filename 'Dockerfile'
                    dir 'backend'
                    label 'nestjs-app'
                }
            }

            steps {
                sh '''
                    node --version
                    ls -la
                    docker images
                '''
            }
        }
    }
}